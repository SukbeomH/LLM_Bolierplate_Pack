{
  "version": "2.0.0",
  "description": "MCP Server Registry — 프로젝트에서 사용하는 MCP 서버 목록 및 메타데이터",
  "servers": {
    "codegraph": {
      "description": "CodeGraph Rust CLI — AST 분석, 의존성 추적, 아키텍처 검토",
      "transport": "stdio",
      "repository": "https://github.com/Jakedismo/codegraph-rust",
      "tools": [
        "agentic_context",
        "agentic_impact",
        "agentic_architecture",
        "agentic_quality"
      ]
    },
    "context7": {
      "description": "Context7 — 라이브러리 공식 문서 조회",
      "transport": "http",
      "url": "https://mcp.context7.com/mcp",
      "auth": "CONTEXT7_API_KEY 환경변수 필요",
      "tools": ["resolve-library-id", "query-docs"]
    }
  },
  "notes": {
    "config_location": "실제 MCP 연결 설정은 프로젝트 루트 .mcp.json 참조",
    "env_vars": "API 키 등 시크릿은 ${VAR} 구문으로 환경변수에서 참조 (.mcp.json)",
    "codegraph_install": "https://github.com/Jakedismo/codegraph-rust"
  }
}
