<!--
이 문서는 AI 에이전트가 작업의 기준점으로 삼을 수 있는 범용 프로젝트 명세서 템플릿입니다.
특정 기술 스택에 종속되지 않고, 비즈니스 로직과 요구사항에 집중합니다.
프로젝트에 맞게 이 템플릿을 채워나가세요.
-->

# Project Specification

## 📋 프로젝트 개요

**프로젝트명**: [프로젝트 이름]

**목적**: [프로젝트의 핵심 목적과 가치 제안을 기술 중립적으로 설명]

**핵심 비즈니스 로직**: 
- [비즈니스 로직 1]
- [비즈니스 로직 2]
- [비즈니스 로직 3]

**이유**: 기술 스택에 독립적인 비즈니스 로직을 먼저 정의함으로써, AI가 어떤 기술을 선택하더라도 핵심 요구사항을 놓치지 않도록 합니다.

## 🗄️ 데이터 도메인 및 엔티티 모델링

### 엔티티 정의

각 엔티티는 기술 중립적으로 정의됩니다:

#### [엔티티명 1]
- **속성**:
  - `id`: 고유 식별자
  - `name`: 이름
  - `createdAt`: 생성 시각
- **관계**: [다른 엔티티와의 관계]
- **비즈니스 규칙**: [도메인 특정 규칙]

#### [엔티티명 2]
- **속성**: [속성 목록]
- **관계**: [관계 설명]
- **비즈니스 규칙**: [규칙 설명]

**이유**: 데이터 모델을 먼저 정의함으로써, AI가 데이터베이스 스키마, API 응답 형식, 타입 정의 등을 일관되게 생성할 수 있습니다.

## 🔌 API 인터페이스 명세

### REST API (선택 가능)

#### [엔드포인트 1]
- **Method**: `GET|POST|PUT|DELETE`
- **Path**: `/api/v1/resource`
- **Request**: 
  ```json
  {
    "field1": "value1",
    "field2": "value2"
  }
  ```
- **Response**: 
  ```json
  {
    "data": {},
    "error": null
  }
  ```
- **에러 처리**: [에러 응답 형식]

#### [엔드포인트 2]
- [동일한 형식으로 정의]

### gRPC (선택 가능)

#### [서비스명]
```protobuf
service ServiceName {
  rpc MethodName(Request) returns (Response);
}
```

**이유**: API 인터페이스를 먼저 정의함으로써, 클라이언트와 서버 간의 계약을 명확히 하고, 구현 단계에서 일관성을 유지할 수 있습니다.

## ✅ 단계별 검증 기준 (Acceptance Criteria)

각 기능은 다음 기준을 만족해야 합니다:

### 기능 1: [기능명]

**Given** [전제 조건]
**When** [동작]
**Then** [기대 결과]

**검증 항목**:
- [ ] 단위 테스트 통과
- [ ] 통합 테스트 통과
- [ ] E2E 테스트 통과 (해당하는 경우)
- [ ] 타입 체크 통과
- [ ] 린트 검사 통과
- [ ] 보안 검사 통과

### 기능 2: [기능명]

[동일한 형식으로 정의]

**이유**: 명확한 검증 기준을 제시함으로써, AI가 구현한 코드가 요구사항을 충족하는지 객관적으로 평가할 수 있습니다.

## 🧪 테스트 전략

### 단위 테스트
- **범위**: 비즈니스 로직, 유틸리티 함수
- **목표 커버리지**: [목표 %]

### 통합 테스트
- **범위**: API 엔드포인트, 데이터베이스 연동
- **목표**: 주요 시나리오 커버

### E2E 테스트
- **범위**: 사용자 시나리오 전체 플로우
- **도구**: Playwright 또는 유사 도구

**이유**: 테스트 전략을 명확히 함으로써, AI가 적절한 수준의 테스트를 작성하고 검증할 수 있습니다.

## 🔐 보안 요구사항

- **인증**: [인증 방식]
- **권한**: [권한 관리 방식]
- **데이터 보호**: [데이터 보호 요구사항]
- **입력 검증**: 모든 외부 입력은 반드시 검증되어야 함

**이유**: 보안 요구사항을 명시함으로써, AI가 보안 취약점을 포함한 코드를 작성하지 않도록 방지합니다.

## 📊 성능 요구사항

- **응답 시간**: [목표 응답 시간]
- **처리량**: [목표 TPS/QPS]
- **동시 사용자**: [목표 동시 사용자 수]

**이유**: 성능 요구사항을 명시함으로써, AI가 성능을 고려한 코드를 작성하도록 유도합니다.

## 🚀 배포 전략

- **환경**: 개발, 스테이징, 프로덕션
- **배포 방식**: [CI/CD 파이프라인 설명]
- **롤백 전략**: [롤백 방법]

**이유**: 배포 전략을 명시함으로써, AI가 배포 가능한 코드를 작성하고 적절한 설정 파일을 생성할 수 있습니다.

---

**참고**: 이 명세서는 프로젝트 진행에 따라 지속적으로 업데이트됩니다. AI는 작업 전에 반드시 이 문서를 참조하여 요구사항을 이해해야 합니다.

