# ==========================================
# 🚀 Universal Boilerplate Config (All Options)
# ==========================================

# --- [Core] Project Identity ---
PROJECT_NAME="my_project"
PROJECT_MODE="option_c" # a=manual, b=auto, c=hybrid

# --- [Core] MCP Configuration ---
# Docker connection details
MCP_DOCKER_HOST="localhost"
MCP_CONTEXT_PORT=8080
MCP_CODANNA_PORT=8081

# --- [Core] Logging & Dashboard ---
LOG_DB_PATH=".logs/events.db"
DASHBOARD_PORT=8000
LOG_LEVEL="INFO"

# ==========================================
# 🤖 Option B: Full Auto (LangChain)
# ==========================================
# Only required if using Option B
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
TAVILY_API_KEY=""

# ==========================================
# 🧠 Option C: Hybrid (Orchestrator)
# ==========================================
# Only required if using Option C
# Path to your CLI tool (e.g. "claude", "codex")
CLI_COMMAND_PATH="claude"
# Timeout for CLI execution in seconds
CLI_TIMEOUT=600
# Git branch prefix for automated tasks
GIT_BRANCH_PREFIX="feature/ai-task-"
