# Project Identity
PROJECT_ID=my_project

# code-graph-rag (https://github.com/vitali87/code-graph-rag)
# CODE_GRAPH_RAG_PATH: code-graph-rag 클론 경로 (절대 경로)
CODE_GRAPH_RAG_PATH=${HOME}/code-graph-rag
TARGET_REPO_PATH=.
CYPHER_PROVIDER=google
CYPHER_MODEL=gemini-2.0-flash
CYPHER_API_KEY=your-gemini-api-key

# memory-graph (https://github.com/MemoryGraph/memorygraph)
MEMORY_BACKEND=sqlite
MEMORY_TOOL_PROFILE=extended

# Context7
CONTEXT7_API_KEY=your-key

# Claude Code
# DISABLE_AUTOUPDATER=1   # 패칭된 cli.js 보호: 자동 업데이트 방지 (make patch-prompt 사용 시 활성화)

# TLS Certificate (corporate proxy environments)
# SSL_CERT_FILE=/path/to/ca-bundle.pem
